import re

list_urls = []

def display_results(history):
	for urls in history:
		if urls not 
		print urls



def get_suggestion(search, history):
	search  = re.split(r'\/', search)
	list_urls = []

	history = {'mysite.com': {'users': {123: {'todos': None, 'trombones': None}, 'unicorns': {123: {'abc': None}, 'foo': None}}

	for s in search:
		base = history.get(s, None)
		if base is not None:
			display_results(base)
